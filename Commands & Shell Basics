# 🐚 Commands & Shell Basics

This module covers fundamental terminal navigation, help documentation utilities, file operations, archiving, and shell operators.

---

## 📚 Module Curriculum

- [x] **Help Commands** (`man`, `help`, `--help`, `whatis`, `info`)
- [ ] **System Information Commands** (`uname`, `hostname`, `uptime`, `whoami`)
- [ ] **Content Viewing Commands** (`cat`, `head`, `tail`, `less`, `more`)
- [ ] **File & Directory Commands** (`mkdir`, `rm`, `cp`, `mv`, `touch`)
- [ ] **Archiving Commands** (`tar`, `gzip`, `zip`, `unzip`)
- [ ] **Command Operators** (`&&`, `||`, `;`, `|`, `>`, `>>`)

---

## 📌 Help Commands

Methods for accessing documentation and command manuals directly within the terminal.

| Command | Description | Syntax Example |
| :--- | :--- | :--- |
| `man` | Displays the manual page for a command | `man ls` |
| `--help` | Shows brief usage instructions for most CLI tools | `ls --help` |
| `whatis` | Displays a single-line manual description | `whatis pwd` |

### Key Notes
* Press `q` to exit a `man` page view.
* Use `/keyword` inside `man` to search for specific options.

---

## 📌 System Information Commands
*(Notes will be added here as I progress...)*

---

## 📌 Content Viewing Commands
*(Notes will be added here as I progress...)*
